package com.horizons.tiles

// Tile #4: Read-back. AUTO-FIRES on every on-device VLM reply (live-chat mode by default).
// The tile is the kill switch: tap = stop Kokoro / mute current speech.
// Green while speaking, slate when idle.
object ReadBackTile {
    const val ID = "tile_read_back"
}
