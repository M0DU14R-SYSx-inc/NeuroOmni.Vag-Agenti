package com.horizons.ui.settings

import androidx.compose.runtime.Composable

@Composable
fun SettingsDrawer() {
    // Phase 2: cogwheel opens this drawer. General section (theme, default backend, default STT/TTS, permissions status).
    // Phase 7: per-tile dropdowns (Mic, AI-pill, Screen-ask, Read-back) — each picks its STT/TTS and backend.
}
