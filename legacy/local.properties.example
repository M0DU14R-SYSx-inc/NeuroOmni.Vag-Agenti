# Copy to local.properties (git-ignored) and fill in. Do NOT commit the real file.
#
# Android SDK location (Android Studio sets this automatically):
# sdk.dir=/path/to/Android/sdk
#
# Nexa token for the OmniNeural edge model (free signup at nexa.ai).
# Read into BuildConfig.NEXA_TOKEN by app/build.gradle.kts. Leave blank for
# stub-only / CI builds.
NEXA_TOKEN=
